// The hero slogan's live value lives in the database (Slogan table, editable
// from the Admin Dashboard). This constant is the fallback shown when no row
// exists yet, and doubles as the seed's initial value — keeping the default
// in one place instead of duplicated across seed, API, and page code.
export const DEFAULT_SLOGAN = 'Real Bars. Real Beats. Real Eswatini.';